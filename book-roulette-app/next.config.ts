// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["books.google.com", "i.gr-assets.com", "books.googleusercontent.com"],
  },
};

module.exports = nextConfig;
